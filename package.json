{
  "name": "agent-os",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "description": "An AI-native operating system for running teams of autonomous AI agents",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build",
    "build:windows-worker": "node scripts/build-windows-worker-release.mjs",
    "check:types": "tsc --noEmit --project tests/types/tsconfig.json",
    "clean": "tsc --build --clean",
    "test": "vitest run --maxWorkers=4",
    "test:watch": "vitest",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "check:layers": "node scripts/check-layers.mjs",
    "check:native-boundary": "node scripts/check-native-boundary.mjs",
    "check:macos": "sh -c 'cd apps/macos && node_modules/.bin/tsc --build && node_modules/.bin/vite build && cargo check --manifest-path src-tauri/Cargo.toml && node_modules/.bin/tauri build --debug --no-bundle'",
    "demo:core": "pnpm run build && node scripts/core-demo.mjs",
    "verify": "tsc --build && pnpm run check:types && biome check . && node scripts/check-layers.mjs && node scripts/check-native-boundary.mjs && pnpm run test && pnpm run check:macos",
    "experience": "node scripts/start-local-product.mjs",
    "experience:native": "node scripts/start-local-product.mjs --native"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "esbuild": "0.25.12",
    "typescript": "^5.6.0",
    "vitest": "3.2.6"
  }
}
