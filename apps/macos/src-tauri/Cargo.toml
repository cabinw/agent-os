[package]
name = "agent-os-macos"
version = "0.0.0"
description = "Agent OS macOS shell"
authors = ["Agent OS"]
edition = "2024"
rust-version = "1.85"

[lib]
name = "agent_os_macos_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.5", features = [] }
