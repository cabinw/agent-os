{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the Agent OS main window",
  "windows": ["main", "menu-bar"],
  "permissions": ["core:default"]
}
